import { type NextPage } from "next";
import Layout from "../components/Layout";

const Home: NextPage = () => {
  return (
    <Layout>
      <h3>This is the home page</h3>
    </Layout>
  );
};

export default Home;
